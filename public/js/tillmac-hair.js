// @ts-nocheck
console.log('File is loaded');
/*
Change product's texture, based on the selected dropdown option
*/
/* const wigTextures = document.getElementsByClassName('textures');
for (let i = 0; i < wigTextures.length; i++) {
    wigTextures[i].onchange = function (event) {
        let texture = document.getElementsByClassName('product-texture');
        for (let j = i; j < texture.length; j++) {
            if (i === j) {
                // console.log('I is currently : ' + i + ' and J is currently: ' + j);
                texture[j].textContent = event.target.value;
            }
        }
    }
} */


/* const wigLengths = document.getElementsByClassName('lengths');

for (let i = 0; i < wigLengths.length; i++) {
    wigLengths[i].onchange = function () {
        console.log('You have selected a ' + this.options[this.selectedIndex].text + '" wig from product  ' + [i += 1]);
    }
} */

/* ajax.post(){

} */
